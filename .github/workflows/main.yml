name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - id: files
        uses: jitterbit/get-changed-files@v1
      - run: |
          for changed_file in ${{ steps.files.outputs.all }}; do
            echo $changed_file ;
           [[ $changed_file == MyProject/* ]] && echo "RUN_BUILD=true" >> $GITHUB_OUTPUT || echo echo "RUN_BUILD=false" >> $GITHUB_OUTPUT ;
          done
      - name: test
        run: |
          echo "${{ steps.files.outputs.RUN_BUILD }}"
        
      - name: Do shit
        if: ${{ steps.files.outputs.RUN_BUILD }}
        run: echo "${{ steps.files.outputs.RUN_BUILD }}"

        

      

